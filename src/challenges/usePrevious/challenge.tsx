/**
 * Implement a hook usePrevious() to return the previous value.
 * The initial previous value should be undefined.
 *
 * @function usePrevious
 * @template T
 * @param {T} value - The current value for which the previous value is needed.
 * @returns {(T | undefined)} - The previous value of the provided input.
 */
export function usePrevious<T>(value: T): T | undefined {
    // your code here
}
