/**
 * Implement a hook useEffectOnce() which behaves like useEffect but runs only once.
 *
 * @function useEffectOnce
 * @param {EffectCallback} effect - A callback function that will be executed once when the component mounts.
 * @returns {void}
 */
import { EffectCallback } from 'react';

export function useEffectOnce(effect: EffectCallback) {
    // your code here
}
